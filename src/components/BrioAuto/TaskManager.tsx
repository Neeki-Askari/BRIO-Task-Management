import "../../styles/brioAuto.scss";
import type { Task } from "../../ context/contexts";

const TaskManager: React.FC<{ tasks: Task[] }> = () => {
  return <div className="task-manager-container"></div>;
};

export default TaskManager;
